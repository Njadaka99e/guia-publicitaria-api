# El # es un comentario, si hay una cadena que necesita el #
# escribirla entre comillas dobles ""
# no se puede tener un comentario en la misma linea que la variable OJO

# para que funcione correctamente el archivo se debe llamar
# .env
DBUSERNAME=usuario
DBPASSWORD="a#1"
DBDATABASE=hr
DBHOST=localhost
NODE_ENV=dev
PORT=3000

# La carpeta en la cual se guardaran las imagenes
FILE_UPLOAD_PATH= ./public/uploads
# El tamaño maximo de las fotos en kb
MAX_FILE_UPLOAD=4000000
